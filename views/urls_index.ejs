<!DOCTYPE html>
<html lang="en">
  <% include ./partials/_head %>

    <body>
      <% include ./partials/_header %>
        <h2>URL Shortener</h2>
        <ul>
          <% for (var key in urls){ %>
            <li>
              <form method="POST" action="/urls/<%=key%>/delete">
                <p>
                  <%= key %> -->
                    <a href="<%= urls[key].longURL%>">
                      <%=urls[key].longURL%>
                    </a>
                    <% if (urls[key].userID == user.id) {%>
                      <a href="/urls_index">Edit</a>
                      <button name="delete" type="submit">Delete!</button>
                      <%}%>
                </p>
              </form>
              </li>
              <% }%>
        </ul>
        <a href="/urls/new/"> Link to short URL Maker </a></body>

</html>
